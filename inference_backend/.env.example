# Authentication
# API_TOKEN is the bearer token used for simple header auth. If ALLOW_NO_AUTH=false and API_TOKEN is empty,
# requests will be rejected with 401.
API_TOKEN=

# If true, requests without Authorization header are allowed (anonymous access).
# Accepts: "true"/"false"/"1"/"0"/"yes"/"no"
ALLOW_NO_AUTH=true

# Search provider configuration
# Set preferred provider: "auto" | "bing" | "duckduckgo" | "wikipedia"
# - "auto": Use Bing if key present, otherwise fall back to keyless (DuckDuckGo + Wikipedia)
SEARCH_PROVIDER=auto

# API key for Bing Web Search. You can also set SEARCH_API_KEY instead.
# If using SEARCH_PROVIDER=bing, one of these must be provided.
BING_API_KEY=
SEARCH_API_KEY=

# Optional: override Bing endpoint (typically leave blank)
BING_ENDPOINT=

# CORS configuration
# Comma-separated list of allowed origins. Default "*" is permissive for development.
# Example: CORS_ALLOW_ORIGINS=https://yourapp.com,https://staging.yourapp.com
CORS_ALLOW_ORIGINS=*

# Whether to allow credentials (cookies/authorization headers) in CORS requests.
CORS_ALLOW_CREDENTIALS=true

# Comma-separated list of allowed HTTP methods or "*" for all.
CORS_ALLOW_METHODS=*

# Comma-separated list of allowed headers or "*" for all.
CORS_ALLOW_HEADERS=*
