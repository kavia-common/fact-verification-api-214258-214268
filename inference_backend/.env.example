# Example environment configuration for the inference_backend service
# Copy this file to `.env` and adjust values for your environment.
#
# IMPORTANT: Do not commit your real secrets.

# Authentication
# If true, allows requests without Authorization header to proceed as anonymous.
# Accepts: "true"/"false", "1"/"0", "yes"/"no" (case-insensitive). Default: false.
ALLOW_NO_AUTH=false

# When ALLOW_NO_AUTH is false, incoming requests must provide:
#   Authorization: Bearer <API_TOKEN>
# If unset and ALLOW_NO_AUTH=false, requests will be rejected with 401.
API_TOKEN=change-me

# Search configuration
# Preferred provider: bing | duckduckgo | wikipedia | auto
# - "auto" (default) uses Bing if an API key is present, otherwise falls back to DuckDuckGo+Wikipedia composite.
SEARCH_PROVIDER=auto

# Bing Search configuration (only needed if using Bing)
# Either BING_API_KEY or SEARCH_API_KEY can be set with the Bing subscription key.
BING_API_KEY=
SEARCH_API_KEY=
# Optional: custom Bing endpoint override (rarely needed).
BING_ENDPOINT=

# CORS configuration
# Comma-separated list of allowed origins. Use "*" for any origin (development convenience).
# Example: CORS_ALLOW_ORIGINS=https://example.com,https://app.example.com
CORS_ALLOW_ORIGINS=*
# Allow credentials (cookies/auth headers) to be sent. Default: true
CORS_ALLOW_CREDENTIALS=true
# Comma-separated methods; "*" means all.
CORS_ALLOW_METHODS=*
# Comma-separated headers; "*" means all.
CORS_ALLOW_HEADERS=*
