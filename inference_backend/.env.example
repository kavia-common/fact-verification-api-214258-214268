# Authentication token required by the API when ALLOW_NO_AUTH is not true
API_TOKEN=replace-with-a-secure-random-token

# If set to true, the API will allow requests without Authorization header (anonymous access)
ALLOW_NO_AUTH=false

# Optional search settings (used by services/search.py when applicable)
# BING_API_KEY=
# SEARCH_API_KEY=
# BING_ENDPOINT=
