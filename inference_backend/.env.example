# Fact Verification Inference API - .env.example
#
# Usage:
# 1) Copy this file to ".env" in the same directory.
# 2) Fill in required values and adjust optional ones for your environment.
# 3) Never commit your real ".env" file with secrets to version control.

# ------------------------------------------------------------------------------
# Authentication
# ------------------------------------------------------------------------------
# REQUIRED in production. Simple bearer token used by the API.
# Clients send Authorization: Bearer <API_TOKEN>
API_TOKEN=change-me-in-production

# Optional. When true, requests without Authorization are allowed (anonymous access).
# Recommended: true for local/dev, false for staging/production.
ALLOW_NO_AUTH=true

# ------------------------------------------------------------------------------
# Search Provider Configuration
# ------------------------------------------------------------------------------
# Which search provider to use.
# Options: auto | bing | duckduckgo | wikipedia
# - auto: Use Bing if a key is present; otherwise fall back to keyless providers.
# - bing: Requires BING_API_KEY or SEARCH_API_KEY to be set.
# - duckduckgo: Keyless HTML fallback provider (best-effort scraping).
# - wikipedia: Keyless Wikipedia OpenSearch API.
SEARCH_PROVIDER=auto

# REQUIRED if SEARCH_PROVIDER=bing. API key for Bing Web Search.
# You can use either BING_API_KEY or SEARCH_API_KEY (alias).
BING_API_KEY=

# Optional alias for Bing API key. If BING_API_KEY is unset, SEARCH_API_KEY will be used.
SEARCH_API_KEY=

# Optional. Custom Bing endpoint (default: https://api.bing.microsoft.com/v7.0/search).
BING_ENDPOINT=

# ------------------------------------------------------------------------------
# CORS Configuration
# ------------------------------------------------------------------------------
# Comma-separated list of allowed origins. Use "*" for all (dev only).
# Example: https://app.example.com,https://staging.example.com
CORS_ALLOW_ORIGINS=*

# Allow including credentials (cookies/auth headers) in cross-site requests.
# true for most app integrations; can be false if not needed.
CORS_ALLOW_CREDENTIALS=true

# Comma-separated list of allowed HTTP methods for CORS.
# Use "*" to allow all methods (dev only).
CORS_ALLOW_METHODS=*

# Comma-separated list of allowed headers for CORS.
# Use "*" to allow all headers (dev only).
CORS_ALLOW_HEADERS=*
